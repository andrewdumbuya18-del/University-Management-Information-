# TODO - MIS landing page

- [x] Implement landing page in `index.php` for logged-out users.

- [x] Keep existing behavior: if logged-in, redirect to role dashboard.

- [x] Use existing UI helpers (`url()`, `app_config()`, `e()`) and existing CSS classes.
- [x] Verify page renders without PHP errors (localhost/MIS/).

